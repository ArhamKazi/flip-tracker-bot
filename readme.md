# Flip Tracker Bot

A Python bot that finds cheap Jordan sneakers on eBay (New condition, under $100) and sends alerts to a Discord channel.

## Features
- Searches eBay automatically
- Filters for "New" condition
- Price filter (default < $100)
- Discord alerts with image + link

## Run
pip install -r requirements.txt
python ebay_tracker.py
